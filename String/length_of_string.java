package String;

public class length_of_string {
    
    public static void main(String[] args) {
        
        String name = "Ayan Mandal";

        System.out.println(name.length());

        //in array we use only length as a paranthesis, but in string we use length as function so we used length()
    }
}
